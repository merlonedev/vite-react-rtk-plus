import { combineReducers } from 'redux'

// this will be the root reducer
// import reducers here and combine them, then use this reducer in store
const reducer = combineReducers({})

export default reducer
